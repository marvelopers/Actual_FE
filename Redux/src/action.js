export function setRestaurants(restaurants) {
  return {
    type: "setRestaurants",
    payload: {
      restaurants,
    },
  };
}
// todo delete
export function delxete(restaurants) {
  return {
    type: "setRestaurants",
    payload: {
      restaurants,
    },
  };
}
